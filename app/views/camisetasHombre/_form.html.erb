<%= form_with(model: camisetahombre) do |form| %>
    <% if camisetahombre.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(camisetahombre.errors.count, "error") %> prohibited this clouth from being saved:</h2>
  
        <ul>
          <% camisetahombre.errors.each do |error| %>
            <li><%= error.full_message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>
    <table>
      <tr>
      <div class="field">
        <td><%= form.label :nombre %></td>
        <td><%= form.text_field :name %></td>
      </div>
      </tr>
      <tr>
      <div class="field">
        <td><%= form.label :precio %></td>
        <td><%= form.text_field :price %></td>
      </div>
      </tr>
      <tr>
      <div class="field">
        <td><%= form.label :source %></td>
        <td><%= form.text_field :source %></td>
      </div>
      </tr>
      <tr>
      <div class="actions">
        <td><%= form.submit %></td>
      </div>
      </tr>
    </table>
  <% end %>
  